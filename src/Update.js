export default function Update(props){
    return(
        <>
            <button onClick={props.increment}>increment</button>
            <button onClick={props.increment}>increment</button>

        </>
    )
}